# “Python program - forecast to SELL/BUY Philip Morris International stock”
### Шевцова Дар'я/Батуринська Поліна
## Обрання акції:
#### Ми обрали дані акцій компанії Philip Morris International за останні півроку через графічно великий ріст та падіння акцій упродовж цього часу. Ми припустили, що наш метод визначення точок продажу/купівлі буде найбільш ефективним якраз через сильну волатильність ціни.
## Етапи реалізації проекту:
### Імпортування даних через бібліотеку yf та реалізація методу Moving Average Crossover 
#### Використовуючи бібліотеку yahoo finance, функція history () імпортує обраний користувачем часовий проміжок в pandas.Dataframe.Додаємо до цього проміжку 75 календарних днів та зберігаємо дані у csv.file. Далі функцією rolling.window() розраховуємо швидку та повільну ковзкі, відкидаємо перші 50 днів, бо це період де невистачає даних для розрахунку повільної ковзкої та знову зберігаємо дані. Порівнюємо ковзкі, отримані результати зберігаємо у нову колонку “Indicator”. Функцією astype(int) перетворюємо значення True/False на відповідні 1/0.

#### Щоб визначити коли краще продавати/купляти функцією diff() обробляємо колонку “Indicator” – отримуємо три можливі випадки: 1/0/-1, записуємо їх у колонку ʼʼPositionʼʼ. Ці значення є індикаторами, 1 та -1 відповідають за купівлю та продаж, 0 за утримання акцій. Створюємо колонку лише зі значеннями відмінними від 0, для легшого сприйняття. Обзиваємо їх: Death cross(Sell) та Golden cross(Buy). 
### Функція інвестування грошей
#### Для імітування трейдингу ми розробили функцію simulate_trading(): основна її мета розрахувати прибуток на основі тих грошей які хоче інвестувати користувач(ввод кількості грошей).
``` 
    def simulate_trading(cross_MA):
    money = float(input("Введіть суму інвестиції ($): "))
    cash = money
    shares = 0
    trades = [] 
```
    
#### Напочатоку функція питає клієнта яку суму він(вона) хоче інвестувати, далі відбувається попередній зріз історичних даних із торговими сигналами: через цикл if ми визначаємо який сигнал видає метод і ми або купуємо усі акції:
```         
            if position == 1 and cash > 0:
            shares = cash // price
            cash = 0
            trades.append({
                "Date": date,
                "Action": "BUY",
                "Price": round(price, 2),
                "Shares": round(shares, 4),
                "Cash": round(cash, 2)
            })
```
#### Або продаемо усі акції:
```
            elif position == -1 and shares > 0:
            cash = shares * price
            profit = cash - money
            trades.append({
                "Date": date,
                "Action": "SELL",
                "Price": round(price, 2),
                "Shares": round(shares, 4),
                "Cash": round(cash, 2),
                "Profit": round(profit, 2)
            })
```

#### Усі ітерації записуються у таблицю із датою, ціною акції, кількість куплених-проданих акцій, залишок коштів, сигналом(SELL/BUY), і профітом. Також якщо на кінці терміну залишаються акції ми їх продаємо і отримуємо фінальний звіт: початкова сума, кінцевий вартість портфелю, і прибуток.
```
    trades_df = pd.DataFrame(trades)
    print(trades_df)
    print(f"Початкова сума: {money}")
    print(f"Кінцева сума: {round(cash, 2)}")
    print(f"Прибуток: {round(cash - money, 2)}")
    return trades_df 
```
### Візуалізація даних 
#### Використовуючи бібліотеку matplotlib, будуємо загальний графік роботи: графік ціни акцій впродовж терміну, графіки ковзних, точки сигналів (SELL/BUY). На осі Ax дати були додатково конвертовані в формат "РРРР-ММ-ДД" та збережені під кутом задля оптимізації простору.
#### Для повного розуміння графіку була додана легенда графіку верхній-правій частині графіку. 

## Висновки:
#### Розроблений Python-скрипт успішно виконує повний цикл: від коректного завантаження й обробки даних через yfinance та pandas до анімованої візуалізації сигналів і результатів. За ці пів року стратегія спрацювала в мінус на 25% від початкового депозиту. Результат абсолютно логічний: акція перебувала у падінні (початкова ціна була вищою за підсумкову), а індикаторні стратегії зазвичай зазнають втрат під час стрімких нисхідних трендів. 
#### P.S: А ще був додан котик, чисто для підтримання стану душевної рівноваги ^^
![Cat](just_a_cat.jpg)

